# 🚂 Руководство по деплою Backend на Railway

Пошаговая инструкция по развертыванию FastAPI backend проекта dofa.uno на платформе Railway.

---

## ⚠️ ВАЖНО: Настройка Root Directory

После создания проекта в Railway:
1. Перейдите в **Settings**
2. Найдите **"Root Directory"**
3. Установите значение: **`backend`**
4. Сохраните изменения
5. Railway автоматически перезапустит деплой

**Без этой настройки деплой не будет работать!**

Backend находится в подпапке `/backend`, поэтому Railway должен знать, где искать [`requirements.txt`](backend/requirements.txt), [`railway.json`](backend/railway.json) и [`nixpacks.toml`](backend/nixpacks.toml).

---

## 📋 Предварительные требования

Перед началом убедитесь, что у вас есть:

- ✅ Аккаунт на GitHub
- ✅ Репозиторий проекта: https://github.com/Nowa1/dofa-uno
- ✅ OpenAI API ключ (получить на https://platform.openai.com/api-keys)
- ✅ Все изменения закоммичены и запушены в GitHub

---

## 🎯 Шаг 1: Регистрация на Railway

1. Перейдите на сайт **Railway**: https://railway.app/

2. Нажмите кнопку **"Start a New Project"** или **"Login"**

3. Выберите **"Login with GitHub"**
   - Railway запросит доступ к вашим репозиториям
   - Разрешите доступ для продолжения

4. После успешной авторизации вы попадете в Dashboard Railway

> 💡 **Совет**: Railway предоставляет $5 бесплатных кредитов каждый месяц, чего достаточно для небольших проектов.

---

## 🚀 Шаг 2: Создание нового проекта

1. В Railway Dashboard нажмите **"New Project"**

2. Выберите **"Deploy from GitHub repo"**

3. В списке репозиториев найдите и выберите **`Nowa1/dofa-uno`**
   - Если репозиторий не отображается, нажмите **"Configure GitHub App"** и предоставьте доступ

4. Railway автоматически обнаружит ваш проект и начнет настройку

---

## ⚙️ Шаг 3: Настройка Root Directory

**Важно!** Backend находится в подпапке `/backend`, поэтому нужно указать правильный путь:

1. После выбора репозитория Railway создаст сервис

2. Перейдите в **Settings** (иконка шестеренки) вашего сервиса

3. Найдите секцию **"Service Settings"**

4. В поле **"Root Directory"** введите:
   ```
   backend
   ```

5. Нажмите **"Save"** или кликните вне поля для сохранения

> ⚠️ **Важно**: Без указания Root Directory Railway не найдет файлы [`requirements.txt`](backend/requirements.txt), [`Procfile`](backend/Procfile) и [`railway.json`](backend/railway.json)

---

## 🔐 Шаг 4: Настройка переменных окружения

Теперь нужно добавить необходимые переменные окружения:

1. В настройках сервиса перейдите на вкладку **"Variables"**

2. Нажмите **"New Variable"** и добавьте следующие переменные:

### Обязательные переменные:

#### `OPENAI_API_KEY`
```
OPENAI_API_KEY=sk-proj-ваш_реальный_ключ_openai
```
- Получите ключ на https://platform.openai.com/api-keys
- Замените `sk-proj-ваш_реальный_ключ_openai` на ваш настоящий ключ

#### `ALLOWED_ORIGINS`
```
ALLOWED_ORIGINS=https://your-frontend-url.vercel.app,http://localhost:5173
```
- Замените `your-frontend-url.vercel.app` на реальный URL вашего frontend на Vercel
- Можно добавить несколько URL через запятую
- Пример: `https://dofa-uno.vercel.app,http://localhost:5173,http://localhost:5174`

#### `PORT` (опционально)
```
PORT=${{RAILWAY_PORT}}
```
- Railway автоматически предоставляет переменную `RAILWAY_PORT`
- Эта настройка уже есть в [`railway.json`](backend/railway.json), но можно добавить для явности

### Как добавить переменные:

1. Нажмите **"+ New Variable"**
2. В поле **"Variable Name"** введите имя (например, `OPENAI_API_KEY`)
3. В поле **"Value"** введите значение
4. Нажмите **"Add"**
5. Повторите для каждой переменной

> 🔒 **Безопасность**: Никогда не коммитьте API ключи в Git! Railway хранит их безопасно.

---

## 💾 Шаг 5: Настройка Volume для SQLite базы данных

SQLite хранит данные в файле, который нужно сохранять между деплоями:

1. В настройках сервиса перейдите на вкладку **"Volumes"** (или **"Data"**)

2. Нажмите **"+ New Volume"**

3. Настройте Volume:
   - **Mount Path**: `/app/backend`
   - **Name**: можно оставить автоматическое имя или назвать `dofa-db`

4. Нажмите **"Add"** или **"Create"**

### Зачем нужен Volume?

- 📁 SQLite создает файл [`database.db`](backend/database.py) в папке `/app/backend`
- 🔄 Без Volume этот файл удаляется при каждом новом деплое
- 💾 Volume сохраняет данные между деплоями и перезапусками

> 💡 **Примечание**: Файл базы данных будет создан автоматически при первом запуске благодаря функции `init_db()` в [`main.py`](backend/main.py:56-61)

---

## 🌐 Шаг 6: Настройка публичного домена

Railway автоматически генерирует домен для вашего сервиса:

1. Перейдите в **Settings** → **Networking**

2. В секции **"Public Networking"** нажмите **"Generate Domain"**

3. Railway создаст URL вида:
   ```
   https://dofa-uno-backend-production.up.railway.app
   ```

4. **Скопируйте этот URL** - он понадобится для настройки frontend!

### Альтернатива: Custom Domain (опционально)

Если у вас есть свой домен:

1. В той же секции **"Public Networking"** нажмите **"Custom Domain"**
2. Введите ваш домен (например, `api.dofa.uno`)
3. Настройте DNS записи согласно инструкциям Railway
4. Дождитесь активации SSL сертификата

---

## ✅ Шаг 7: Проверка деплоя

После настройки Railway автоматически начнет деплой:

### 7.1 Мониторинг деплоя

1. Перейдите на вкладку **"Deployments"**
2. Вы увидите процесс сборки и деплоя в реальном времени
3. Дождитесь статуса **"Success"** (зеленая галочка ✅)

### 7.2 Проверка логов

1. Перейдите на вкладку **"Logs"** или **"View Logs"**
2. Вы должны увидеть:
   ```
   Initializing database...
   Database initialized successfully!
   INFO:     Started server process
   INFO:     Uvicorn running on http://0.0.0.0:XXXX
   ```

### 7.3 Тестирование API

1. Откройте в браузере ваш Railway URL + `/docs`:
   ```
   https://your-backend-url.railway.app/docs
   ```

2. Вы должны увидеть **Swagger UI** с документацией API

3. Протестируйте основные эндпоинты:

   #### Проверка здоровья:
   - Откройте: `https://your-backend-url.railway.app/health`
   - Ожидаемый ответ:
     ```json
     {
       "status": "healthy",
       "openai_configured": true
     }
     ```

   #### Проверка корневого эндпоинта:
   - Откройте: `https://your-backend-url.railway.app/`
   - Ожидаемый ответ:
     ```json
     {
       "message": "Dofa.uno API is running"
     }
     ```

   #### Тест парсинга задач (через Swagger UI):
   - Перейдите в `/docs`
   - Найдите эндпоинт **POST** [`/api/parse_dump`](backend/main.py:215-306)
   - Нажмите **"Try it out"**
   - Введите тестовый текст:
     ```json
     {
       "text": "Нужно ответить на email и исправить баг в коде"
     }
     ```
   - Нажмите **"Execute"**
   - Проверьте, что получили список задач

---

## 🐛 Шаг 8: Troubleshooting (Решение проблем)

### Проблема 1: Деплой падает с ошибкой "Module not found"

**Причина**: Не указан Root Directory или неправильный путь

**Решение**:
1. Settings → Service Settings → Root Directory
2. Убедитесь, что указано: `backend`
3. Сохраните и переделайте деплой

---

### Проблема 2: "OpenAI API key not configured"

**Причина**: Не добавлена переменная окружения `OPENAI_API_KEY`

**Решение**:
1. Settings → Variables
2. Добавьте переменную `OPENAI_API_KEY` с вашим ключом
3. Railway автоматически перезапустит сервис

---

### Проблема 3: CORS ошибки при запросах с frontend

**Причина**: Frontend URL не добавлен в `ALLOWED_ORIGINS`

**Решение**:
1. Settings → Variables
2. Найдите переменную `ALLOWED_ORIGINS`
3. Добавьте URL вашего frontend через запятую:
   ```
   https://your-frontend.vercel.app,http://localhost:5173
   ```
4. Сохраните изменения

**Проверка**: В [`main.py`](backend/main.py:29-40) настроен CORS middleware, который использует эту переменную

---

### Проблема 4: База данных пустая после редеплоя

**Причина**: Не настроен Volume для сохранения SQLite файла

**Решение**:
1. Settings → Volumes → New Volume
2. Mount Path: `/app/backend`
3. После создания Volume данные будут сохраняться

---

### Проблема 5: Сервис не запускается

**Причина**: Ошибка в коде или зависимостях

**Решение**:
1. Проверьте логи: Deployments → View Logs
2. Найдите строку с ошибкой (обычно красным цветом)
3. Частые причины:
   - Отсутствует зависимость в [`requirements.txt`](backend/requirements.txt)
   - Синтаксическая ошибка в коде
   - Неправильная версия Python (должна быть 3.11 согласно [`runtime.txt`](backend/runtime.txt))

---

### Как смотреть логи в реальном времени

1. Откройте ваш сервис в Railway
2. Перейдите на вкладку **"Logs"**
3. Логи обновляются автоматически
4. Используйте фильтры для поиска ошибок

---

### Полезные команды для отладки

Railway предоставляет CLI для локальной отладки:

```bash
# Установка Railway CLI
npm i -g @railway/cli

# Логин
railway login

# Подключение к проекту
railway link

# Просмотр логов
railway logs

# Запуск локально с переменными Railway
railway run python -m uvicorn main:app --reload
```

---

## 🎉 Шаг 9: Следующие шаги

После успешного деплоя backend:

### 1. Сохраните Railway Backend URL

Скопируйте ваш Railway URL (например, `https://dofa-uno-backend-production.up.railway.app`)

### 2. Обновите Frontend конфигурацию

В файле [`frontend/src/utils/api.js`](frontend/src/utils/api.js) обновите `API_BASE_URL`:

```javascript
const API_BASE_URL = import.meta.env.VITE_API_URL || 'https://your-railway-url.railway.app';
```

### 3. Добавьте переменную окружения в Vercel

При деплое frontend на Vercel добавьте:
```
VITE_API_URL=https://your-railway-url.railway.app
```

### 4. Обновите ALLOWED_ORIGINS в Railway

После деплоя frontend на Vercel, добавьте Vercel URL в `ALLOWED_ORIGINS`:
```
ALLOWED_ORIGINS=https://your-frontend.vercel.app,http://localhost:5173
```

### 5. Переходите к деплою Frontend

Следуйте инструкциям в файле [`DEPLOYMENT.md`](DEPLOYMENT.md) для деплоя frontend на Vercel.

---

## 📊 Мониторинг и обслуживание

### Просмотр метрик

Railway предоставляет метрики использования:

1. Откройте ваш сервис
2. Перейдите на вкладку **"Metrics"**
3. Отслеживайте:
   - 📈 CPU usage
   - 💾 Memory usage
   - 🌐 Network traffic
   - 💰 Estimated cost

### Управление деплоями

- **Откат к предыдущей версии**: Deployments → выберите старый деплой → Redeploy
- **Ручной редеплой**: Deployments → Latest → Redeploy
- **Остановка сервиса**: Settings → Danger Zone → Remove Service

### Резервное копирование базы данных

Для SQLite на Railway:

1. Используйте Railway CLI:
   ```bash
   railway run python -c "import shutil; shutil.copy('database.db', 'backup.db')"
   ```

2. Или настройте автоматический бэкап через cron job

---

## 🔗 Полезные ссылки

- 📚 [Railway Documentation](https://docs.railway.app/)
- 🐍 [FastAPI Documentation](https://fastapi.tiangolo.com/)
- 🤖 [OpenAI API Documentation](https://platform.openai.com/docs)
- 📦 [Проект на GitHub](https://github.com/Nowa1/dofa-uno)

---

## 📝 Конфигурационные файлы проекта

Ваш backend уже настроен для Railway благодаря этим файлам:

- [`backend/railway.json`](backend/railway.json) - конфигурация Railway (builder, start command)
- [`backend/Procfile`](backend/Procfile) - команда запуска для Railway
- [`backend/runtime.txt`](backend/runtime.txt) - версия Python (3.11)
- [`backend/requirements.txt`](backend/requirements.txt) - зависимости Python
- [`backend/.env.example`](backend/.env.example) - пример переменных окружения

---

## ✨ Готово!

Ваш backend успешно развернут на Railway! 🎊

**Что дальше?**
- ✅ Backend работает на Railway
- 🔜 Деплой Frontend на Vercel (см. [`DEPLOYMENT.md`](DEPLOYMENT.md))
- 🚀 Подключение Frontend к Backend
- 🎮 Тестирование полного приложения

---

## 💬 Нужна помощь?

Если возникли проблемы:

1. Проверьте логи в Railway
2. Убедитесь, что все переменные окружения настроены
3. Проверьте, что Root Directory = `backend`
4. Убедитесь, что Volume создан для базы данных
5. Проверьте документацию Railway: https://docs.railway.app/

**Удачи с деплоем! 🚀**
