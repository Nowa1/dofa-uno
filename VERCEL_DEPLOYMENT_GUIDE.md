# 🚀 Керівництво по деплою Frontend на Vercel

Детальна інструкція по розгортанню React + Vite + Tailwind CSS застосунку dofa.uno на платформі Vercel.

---

## 📋 Передумови

Перед початком переконайтеся, що у вас є:
- ✅ Акаунт на GitHub
- ✅ Репозиторій проекту: [Nowa1/dofa-uno](https://github.com/Nowa1/dofa-uno)
- ✅ Backend задеплоєно на: `https://api.dopa.uno`
- ✅ Доступ до налаштувань репозиторію

---

## 1️⃣ Реєстрація на Vercel

### Крок 1.1: Створення акаунту

1. Перейдіть на [vercel.com](https://vercel.com/)
2. Натисніть **"Sign Up"** у правому верхньому куті
3. Оберіть **"Continue with GitHub"**
4. Авторизуйте Vercel для доступу до вашого GitHub акаунту
5. Підтвердіть email адресу (якщо потрібно)

### Крок 1.2: Налаштування профілю

1. Після входу ви побачите Dashboard Vercel
2. Можете налаштувати ім'я профілю та аватар (опціонально)

---

## 2️⃣ Імпорт проекту з GitHub

### Крок 2.1: Створення нового проекту

1. На головній сторінці Vercel натисніть **"Add New..."**
2. Оберіть **"Project"** з випадаючого меню
3. Ви побачите список ваших GitHub репозиторіїв

### Крок 2.2: Вибір репозиторію

1. Знайдіть репозиторій **"Nowa1/dofa-uno"** у списку
2. Якщо не бачите репозиторій:
   - Натисніть **"Adjust GitHub App Permissions"**
   - Надайте Vercel доступ до потрібного репозиторію
   - Поверніться назад та оновіть список
3. Натисніть **"Import"** біля репозиторію dofa-uno

### Крок 2.3: Налаштування проекту

На сторінці конфігурації проекту:

1. **Project Name**: залишіть `dofa-uno` або змініть на свій варіант
2. **Framework Preset**: оберіть **"Vite"** зі списку
3. **Root Directory**: натисніть **"Edit"** та вкажіть `frontend`
   - ⚠️ Це критично важливо, оскільки frontend код знаходиться в папці `/frontend`

---

## 3️⃣ Налаштування Build Settings

Vercel автоматично визначить налаштування для Vite, але перевірте:

### Build & Development Settings

```
Framework Preset: Vite
Build Command: npm run build
Output Directory: dist
Install Command: npm install
Development Command: npm run dev
```

### Детальний опис команд:

- **Build Command** (`npm run build`): 
  - Компілює React код у оптимізовані статичні файли
  - Використовує Vite для швидкого білду
  - Створює production-ready версію

- **Output Directory** (`dist`):
  - Папка, куди Vite складає зібрані файли
  - Vercel буде сервити файли з цієї папки

- **Install Command** (`npm install`):
  - Встановлює всі залежності з [`package.json`](frontend/package.json)
  - Включає React, Vite, Tailwind CSS, Framer Motion, та інші

---

## 4️⃣ Налаштування змінних оточення

### Крок 4.1: Додавання Environment Variables

1. Прокрутіть вниз до секції **"Environment Variables"**
2. Натисніть **"Add"** або розгорніть секцію
3. Додайте наступну змінну:

```
Name:  VITE_API_URL
Value: https://api.dopa.uno
```

### Крок 4.2: Налаштування для різних середовищ

Ви можете налаштувати різні значення для:
- **Production**: основний деплой (main branch)
- **Preview**: тестові деплої (pull requests)
- **Development**: локальна розробка

Для початку достатньо налаштувати для всіх середовищ однаково.

### ⚠️ Важливо про змінні оточення:

- Всі змінні для Vite повинні починатися з `VITE_`
- Змінні доступні в коді через `import.meta.env.VITE_API_URL`
- Після зміни змінних потрібен редеплой

---

## 5️⃣ Deploy проекту

### Крок 5.1: Запуск деплою

1. Перевірте всі налаштування ще раз
2. Натисніть велику синю кнопку **"Deploy"**
3. Vercel почне процес деплою

### Крок 5.2: Процес білду

Ви побачите лог білду в реальному часі:

```
Installing dependencies...
✓ Dependencies installed (30s)

Building application...
✓ Build completed (45s)

Deploying to Vercel...
✓ Deployment ready (15s)
```

**Очікуваний час**: 2-4 хвилини

### Крок 5.3: Успішний деплой

Після завершення ви побачите:
- ✅ Зелену галочку "Deployment Ready"
- 🔗 URL вашого застосунку (наприклад: `dofa-uno.vercel.app`)
- 🎉 Конфетті на екрані (якщо це перший деплой)

---

## 6️⃣ Перевірка деплою

### Крок 6.1: Відкриття застосунку

1. Натисніть на URL або кнопку **"Visit"**
2. Застосунок відкриється в новій вкладці

### Крок 6.2: Тестування функціоналу

Перевірте наступне:

✅ **Візуальна перевірка:**
- Сторінка завантажується без помилок
- Стилі Tailwind CSS застосовані коректно
- Анімації працюють (Framer Motion)
- Mascot відображається правильно

✅ **Підключення до API:**
- Відкрийте Developer Tools (F12)
- Перейдіть на вкладку Network
- Виконайте дію, що викликає API запит
- Перевірте, що запити йдуть на `https://api.dopa.uno`
- Статус відповідей має бути 200 OK

✅ **Основний функціонал:**
- Створення завдань
- Відображення статистики
- Gamification елементи (XP, рівні, досягнення)
- NeuroTunnel анімація
- Звукові ефекти

### Крок 6.3: Перевірка консолі

У Developer Tools → Console не повинно бути:
- ❌ CORS помилок
- ❌ 404 помилок для API
- ❌ JavaScript помилок

---

## 7️⃣ Налаштування Custom Domain (опціонально)

### Крок 7.1: Додавання домену

1. У Dashboard проекту перейдіть на вкладку **"Settings"**
2. Оберіть **"Domains"** в лівому меню
3. Натисніть **"Add"**
4. Введіть ваш домен: `dofa.uno` або `dopa.uno`
5. Натисніть **"Add"**

### Крок 7.2: Налаштування DNS

Vercel покаже інструкції для налаштування DNS. Вам потрібно додати записи:

**Для apex domain (dofa.uno):**
```
Type: A
Name: @
Value: 76.76.21.21
```

**Для www subdomain:**
```
Type: CNAME
Name: www
Value: cname.vercel-dns.com
```

### Крок 7.3: Очікування верифікації

- DNS зміни можуть зайняти від 5 хвилин до 48 годин
- Vercel автоматично перевірить налаштування
- Після верифікації домен стане активним
- SSL сертифікат буде виданий автоматично

---

## 8️⃣ Автоматичні деплої

### 🎯 Як це працює:

Vercel автоматично інтегрується з GitHub:

**Production деплої:**
- Кожен `git push` в гілку `main` автоматично деплоїться
- URL: ваш основний домен (наприклад, `dofa-uno.vercel.app`)
- Це ваша production версія

**Preview деплої:**
- Кожен Pull Request отримує унікальний preview URL
- Формат: `dofa-uno-git-branch-name-username.vercel.app`
- Ідеально для тестування змін перед мерджем
- Автоматично оновлюється при кожному push в PR

**Branch деплої:**
- Кожна гілка може мати свій preview URL
- Корисно для тестування feature branches

### 📝 Workflow:

```
1. Створюєте нову гілку: git checkout -b feature/new-feature
2. Робите зміни та commit: git commit -m "Add new feature"
3. Push в GitHub: git push origin feature/new-feature
4. Vercel автоматично створює preview деплой
5. Отримуєте коментар в GitHub з посиланням на preview
6. Тестуєте зміни на preview URL
7. Мерджите PR в main
8. Vercel автоматично деплоїть в production
```

### 🔔 Нотифікації:

Vercel може надсилати нотифікації про деплої:
- Email
- Slack
- Discord
- GitHub коментарі

Налаштування: Settings → Git → Deploy Hooks

---

## 9️⃣ Troubleshooting

### 🔍 Як дивитися логи білду

1. Перейдіть на вкладку **"Deployments"**
2. Оберіть потрібний деплой зі списку
3. Натисніть на нього для перегляду деталей
4. Вкладка **"Building"** показує логи білду
5. Вкладка **"Functions"** показує runtime логи (якщо є)

### ❌ Часті проблеми та рішення

#### Проблема 1: Build Failed - Dependencies Error

**Симптоми:**
```
npm ERR! code ERESOLVE
npm ERR! ERESOLVE unable to resolve dependency tree
```

**Рішення:**
1. Перевірте [`package.json`](frontend/package.json) на конфлікти версій
2. Спробуйте змінити Install Command на: `npm install --legacy-peer-deps`
3. Або: `npm ci --legacy-peer-deps`

#### Проблема 2: Build Failed - Out of Memory

**Симптоми:**
```
FATAL ERROR: Reached heap limit Allocation failed
```

**Рішення:**
1. Додайте змінну оточення: `NODE_OPTIONS=--max_old_space_size=4096`
2. Оптимізуйте імпорти (використовуйте tree-shaking)
3. Перевірте, чи немає циклічних залежностей

#### Проблема 3: CORS Errors

**Симптоми:**
```
Access to fetch at 'https://api.dopa.uno' from origin 'https://dofa-uno.vercel.app' 
has been blocked by CORS policy
```

**Рішення:**
1. Перевірте backend налаштування CORS
2. Переконайтеся, що ваш Vercel домен додано до allowed origins
3. У FastAPI backend додайте:
```python
from fastapi.middleware.cors import CORSMiddleware

app.add_middleware(
    CORSMiddleware,
    allow_origins=[
        "https://dofa-uno.vercel.app",
        "https://your-custom-domain.com"
    ],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

#### Проблема 4: Environment Variables Not Working

**Симптоми:**
- `import.meta.env.VITE_API_URL` повертає `undefined`
- API запити йдуть на неправильний URL

**Рішення:**
1. Перевірте, що змінна починається з `VITE_`
2. Після додавання змінних зробіть редеплой:
   - Deployments → три крапки → Redeploy
3. Перевірте, що змінна додана для правильного environment (Production/Preview)

#### Проблема 5: 404 on Page Refresh

**Симптоми:**
- Головна сторінка працює
- При переході на `/dashboard` все ОК
- При refresh на `/dashboard` → 404

**Рішення:**
Це вже вирішено в [`vercel.json`](vercel.json:1-13):
```json
{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
```

Якщо проблема залишається:
1. Перевірте, що [`vercel.json`](vercel.json:1-13) в root директорії проекту
2. Зробіть редеплой

#### Проблема 6: Slow Build Times

**Симптоми:**
- Білд займає більше 5 хвилин
- Timeout errors

**Рішення:**
1. Оптимізуйте залежності (видаліть непотрібні)
2. Використовуйте `npm ci` замість `npm install`
3. Додайте `.vercelignore` для виключення непотрібних файлів:
```
node_modules
.git
*.log
.env.local
```

#### Проблема 7: API Connection Timeout

**Симптоми:**
```
Failed to fetch
Network request failed
```

**Рішення:**
1. Перевірте, що backend працює: `curl https://api.dopa.uno/health`
2. Перевірте змінну `VITE_API_URL` в Vercel
3. Перевірте Network tab в DevTools для деталей помилки
4. Можливо, потрібно збільшити timeout в API клієнті

### 🆘 Додаткова допомога

Якщо проблема не вирішена:
1. Перевірте [Vercel Documentation](https://vercel.com/docs)
2. Перегляньте [Vercel Community](https://github.com/vercel/vercel/discussions)
3. Перевірте логи в реальному часі: `vercel logs <deployment-url>`

---

## 🔟 Наступні кроки

### 📊 Налаштування Analytics

Vercel надає вбудовану аналітику:

1. Перейдіть на вкладку **"Analytics"**
2. Увімкніть **Vercel Analytics**:
   - Додайте в [`main.jsx`](frontend/src/main.jsx):
   ```javascript
   import { inject } from '@vercel/analytics';
   inject();
   ```
3. Встановіть пакет:
   ```bash
   npm install @vercel/analytics
   ```
4. Зробіть commit та push

**Що ви отримаєте:**
- Кількість відвідувачів
- Page views
- Top pages
- Географія користувачів
- Device breakdown

### ⚡ Оптимізація Performance

#### 1. Увімкніть Speed Insights

```bash
npm install @vercel/speed-insights
```

```javascript
import { SpeedInsights } from '@vercel/speed-insights/react';

function App() {
  return (
    <>
      <YourApp />
      <SpeedInsights />
    </>
  );
}
```

#### 2. Налаштуйте Image Optimization

Якщо використовуєте зображення:
- Використовуйте WebP формат
- Додайте lazy loading
- Оптимізуйте розміри

#### 3. Code Splitting

Vite автоматично робить code splitting, але можна покращити:
```javascript
const Dashboard = lazy(() => import('./components/Dashboard'));
```

#### 4. Caching Strategy

Vercel автоматично кешує статичні файли, але можна налаштувати:
```json
// vercel.json
{
  "headers": [
    {
      "source": "/assets/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=31536000, immutable"
        }
      ]
    }
  ]
}
```

### 🔄 Налаштування Redirects

Додайте в [`vercel.json`](vercel.json:1-13):

```json
{
  "redirects": [
    {
      "source": "/old-path",
      "destination": "/new-path",
      "permanent": true
    }
  ]
}
```

### 🔒 Security Headers

Додайте security headers для захисту:

```json
{
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        },
        {
          "key": "X-Frame-Options",
          "value": "DENY"
        },
        {
          "key": "X-XSS-Protection",
          "value": "1; mode=block"
        }
      ]
    }
  ]
}
```

### 🌍 Edge Functions (Advanced)

Для складніших сценаріїв можна використовувати Edge Functions:
- Middleware для аутентифікації
- A/B тестування
- Геолокація
- Rate limiting

### 📱 PWA Support

Додайте Progressive Web App функціонал:
1. Встановіть `vite-plugin-pwa`
2. Налаштуйте service worker
3. Додайте manifest.json
4. Vercel автоматично підтримує PWA

---

## 📚 Корисні посилання

- 📖 [Vercel Documentation](https://vercel.com/docs)
- 🎓 [Vite Documentation](https://vitejs.dev/)
- ⚛️ [React Documentation](https://react.dev/)
- 🎨 [Tailwind CSS Documentation](https://tailwindcss.com/docs)
- 🔧 [Vercel CLI](https://vercel.com/docs/cli)
- 💬 [Vercel Community](https://github.com/vercel/vercel/discussions)

---

## 🎉 Вітаємо!

Ваш frontend успішно задеплоєно на Vercel! 

**Що далі:**
- ✅ Протестуйте всі функції
- ✅ Налаштуйте custom domain
- ✅ Увімкніть analytics
- ✅ Оптимізуйте performance
- ✅ Налаштуйте monitoring

**Автоматизація:**
- Кожен push в main автоматично деплоїться
- Pull requests отримують preview URLs
- Rollback доступний в один клік

---

## 📞 Підтримка

Якщо виникли питання:
1. Перевірте цей гайд ще раз
2. Перегляньте логи деплою
3. Перевірте документацію Vercel
4. Створіть issue в GitHub репозиторії

**Happy Deploying! 🚀**
